int i = a*b;
int func(int x, char *b);
int f (int b) {
    int b = 9;
    int i = a*b;  
        int i = ab() + b + 2*(b+c);
        char *s = "hello";
        // hello
        /* hello world
        hi
        */
        func();
        if ( a == b) {
            i = b;
            while (b<(a+b)) {
                s == b;
            }
        }
        else {
            int x = s;

        }
}
int x(int a,int b){
    
        int i = a*b;  
        int i = ab() + b + 2*(b+c);
        char *s = "hello";
        // hello
        /* hello world
        hi
        */
        func();
        if ( a == b) {
            i = b;
            while (b<(a+b)) {
                s == b;
            }
        }
        else {
            int x = s;

        }
        return a&b + fun(a+b,c+d*(b+c));
}